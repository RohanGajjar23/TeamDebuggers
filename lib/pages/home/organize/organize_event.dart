import 'package:flutter/material.dart';

class OrganizeEvents extends StatelessWidget {
  const OrganizeEvents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
